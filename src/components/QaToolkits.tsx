import { useMemo, useState } from 'react'
import { DeepDiveArticle } from './DeepDiveArticle'

const promptLibrary = [
  { category: 'Requirements', label: 'Requirement review', when: 'Use before testing begins.', paste: 'Paste a user story, acceptance criteria, or feature notes.', outcome: 'A list of assumptions, missing criteria, risks, and questions.', prompt: 'Act as a QA engineer reviewing this requirement: [PASTE REQUIREMENT]. Identify unclear acceptance criteria, assumptions, user risks, edge cases, accessibility considerations, integration concerns, and the most useful questions to clarify before testing. Return the findings in priority order.' },
  { category: 'Requirements', label: 'Acceptance criteria helper', when: 'Use when a story is too broad to test confidently.', paste: 'Paste the story and any known business rules.', outcome: 'Clear, testable acceptance criteria and open questions.', prompt: 'Turn this feature request into testable acceptance criteria: [PASTE STORY]. Include the intended user outcome, happy path, important failure states, permissions or data conditions, accessibility expectations, and questions that still need product clarification. Do not invent product rules; label assumptions clearly.' },
  { category: 'Test design', label: 'Risk-based test plan', when: 'Use when planning coverage for a feature or release.', paste: 'Paste the feature, user types, and known changes.', outcome: 'Prioritized coverage, risks, and a starting test approach.', prompt: 'Act as a senior QA engineer. Based on this feature: [PASTE FEATURE], create a risk-based test approach. Include the highest-risk user journeys, happy paths, negative paths, boundary cases, data needs, accessibility checks, API or integration concerns, and questions to clarify. Prioritize the list by user and business impact.' },
  { category: 'Test design', label: 'Exploratory test charter', when: 'Use before a focused exploratory session.', paste: 'Paste the feature, change, or user problem to explore.', outcome: 'A time-boxed mission, ideas to investigate, and evidence to capture.', prompt: 'Create a 45-minute exploratory testing charter for: [PASTE FEATURE OR CHANGE]. Provide a clear mission, the user or risk to focus on, test ideas to explore, useful data or environments, and the evidence to capture. Keep the plan focused enough for one session.' },
  { category: 'Automation', label: 'Automation candidate check', when: 'Use when deciding whether to automate a workflow.', paste: 'Paste the workflow, frequency, risk, and known setup needs.', outcome: 'A recommendation with trade-offs and a small first automation step.', prompt: 'Evaluate whether this workflow is a good automation candidate: [PASTE WORKFLOW]. Consider repetition, business or user risk, behavior stability, test data, setup complexity, maintenance cost, and feedback value. Recommend automate, explore manually, or use a blended approach. Explain the first useful step without promising unrealistic coverage.' },
  { category: 'Automation', label: 'Automation test outline', when: 'Use before writing a Playwright, Selenium, or REST Assured test.', paste: 'Paste the user journey or API behavior and chosen tool.', outcome: 'Readable test intent, setup, assertions, and reliability concerns.', prompt: 'Outline a maintainable automated test for [PASTE JOURNEY OR API] using [PLAYWRIGHT / SELENIUM / REST ASSURED]. Include test intent, preconditions, stable test data, key actions, meaningful assertions, cleanup, failure diagnostics, and likely sources of flakiness. Do not write production code unless requested.' },
  { category: 'API testing', label: 'API coverage ideas', when: 'Use when learning or expanding an endpoint test plan.', paste: 'Paste the endpoint, method, expected contract, and auth details.', outcome: 'Request, response, validation, and failure coverage ideas.', prompt: 'Create API test ideas for: [METHOD AND ENDPOINT]. Context: [PASTE CONTRACT, AUTH, AND BUSINESS RULES]. Include valid requests, required and optional fields, invalid or boundary data, authentication and authorization, response schema, error handling, idempotency or duplicate requests where relevant, and dependency failures. Separate must-have checks from useful follow-ups.' },
  { category: 'API testing', label: 'API defect investigation', when: 'Use after an unexpected response or integration failure.', paste: 'Paste the request, response, environment, and observed behavior.', outcome: 'A disciplined list of checks and evidence—not guesses.', prompt: 'Help investigate this API issue without guessing. Request: [PASTE REQUEST]. Response or error: [PASTE RESPONSE]. Environment and context: [PASTE DETAILS]. Suggest the most useful next checks involving inputs, authentication, headers, logs, downstream services, data state, and comparison environments. State what evidence would confirm each likely cause.' },
  { category: 'Accessibility', label: 'Accessible flow checklist', when: 'Use while testing a workflow with forms, dialogs, or status changes.', paste: 'Paste the user flow and main UI elements.', outcome: 'Keyboard, focus, labels, errors, and status checks.', prompt: 'Create a practical accessibility test checklist for this flow: [PASTE FLOW]. Cover keyboard-only use, focus order and visible focus, semantic labels, form errors, dialogs, status messages, color or contrast risks, zoom and responsive behavior, and what to verify with assistive technology. Keep the checklist focused on observable behavior.' },
  { category: 'Accessibility', label: 'Accessibility defect helper', when: 'Use when documenting an accessibility issue clearly.', paste: 'Paste what happened, expected behavior, and the environment.', outcome: 'A respectful, reproducible defect report with impact.', prompt: 'Turn these notes into a clear accessibility defect report: [PASTE NOTES]. Include a concise title, environment, reproduction steps, actual and expected behavior, likely user impact, relevant accessibility context, and evidence to attach. Avoid claiming a formal compliance violation unless the supplied evidence supports it.' },
  { category: 'Delivery', label: 'Release readiness', when: 'Use before a release or a high-risk production change.', paste: 'Paste the release scope, changes, and known risks.', outcome: 'A concise readiness conversation and follow-up plan.', prompt: 'Create a concise release-readiness checklist for: [PASTE FEATURE OR RELEASE]. Include functional coverage, regression risk, integrations, accessibility, known gaps, monitoring, support ownership, rollback or recovery considerations, and stakeholder communication. Separate release blockers from follow-up work.' },
  { category: 'Delivery', label: 'AI feature evaluation', when: 'Use when testing an LLM-powered feature or assistant.', paste: 'Paste the feature goal, intended audience, and any guardrails.', outcome: 'A starting evaluation set for usefulness, safety, and reliability.', prompt: 'Create a QA evaluation plan for this AI feature: [PASTE FEATURE AND GUARDRAILS]. Include representative user questions, ambiguous requests, harmful or irrelevant requests, prompt-injection attempts, incorrect or unsupported answers, privacy concerns, consistency checks, escalation behavior, and criteria for human review. Treat the output as a test starting point, not a safety guarantee.' },
] as const

const promptCategories = ['All', ...Array.from(new Set(promptLibrary.map((item) => item.category)))]

const automationQuestions = [
  'Does the flow happen often or on every release?',
  'Would a failure create meaningful user or business risk?',
  'Is the expected behavior stable enough to describe clearly?',
  'Can reliable test data and environments be created or reset?',
  'Can the test run without complex, fragile setup?',
  'Would automated feedback arrive early enough to help the team?',
] as const

const testAreas = ['Functional', 'Negative', 'Accessibility', 'API / Integration', 'Security', 'Regression'] as const
type TestArea = typeof testAreas[number]
type GeneratedCase = { id: string; area: TestArea; title: string; description: string; priority: 'High' | 'Medium'; preconditions: string; steps: string; expected: string }

function buildTestCases(description: string, role: string, platform: string, risk: string, integrations: string, areas: TestArea[]): GeneratedCase[] {
  const featureName = description.replace(/\s+/g, ' ').trim().replace(/[.?!]+$/, '').slice(0, 120) || 'this feature'
  const normalized = description.toLowerCase()
  const user = role.trim() || 'a valid user'
  const environment = platform.trim() || 'the supported application environment'
  const integrationNote = integrations.trim() || 'any supporting APIs, stored data, permissions, or notifications'
  const cases: GeneratedCase[] = []
  const areaDescriptions: Record<TestArea, string> = {
    Functional: 'Confirms that the intended user outcome can be completed successfully.',
    Negative: 'Checks that invalid, interrupted, or unusual behavior is handled clearly and safely.',
    Accessibility: 'Checks that the flow remains understandable and operable for more users.',
    'API / Integration': 'Checks the supporting systems and data exchanges behind the user experience.',
    Security: 'Checks that access, sessions, and sensitive behavior are protected.',
    Regression: 'Checks that the change does not damage important existing behavior.',
  }
  const add = (area: TestArea, title: string, priority: 'High' | 'Medium', preconditions: string, steps: string, expected: string) => cases.push({ id: `${area}-${title}`, area, title, description: `${areaDescriptions[area]} Focus: ${title.toLowerCase()}.`, priority, preconditions, steps, expected })

  if (areas.includes('Functional')) add('Functional', 'Complete the core user journey', 'High', `${user} can access ${environment} with valid data.`, `Complete “${featureName}” using valid information and the intended path.`, 'The user receives a clear success state and the intended result is saved, displayed, or delivered correctly.')
  if (areas.includes('Negative')) add('Negative', 'Validate invalid, missing, and boundary input', 'High', `${user} can start “${featureName}.”`, 'Try missing, malformed, duplicate, and boundary-value input; then repeat a submission or interrupt the flow.', 'The feature prevents invalid behavior, gives useful feedback, and does not lose, duplicate, or misrepresent data.')
  if (areas.includes('Accessibility')) add('Accessibility', 'Complete the flow with keyboard-only use', 'Medium', `${user} can reach the feature without a mouse.`, `Use Tab, Shift+Tab, Enter, Escape, and visible focus to complete “${featureName}.”`, 'Focus order, labels, errors, dialogs, and status updates make the experience understandable and operable.')
  if (areas.includes('API / Integration')) add('API / Integration', 'Verify supporting integration behavior', 'High', `${integrationNote} are available in a test environment.`, `Exercise “${featureName}” while checking request/response behavior, delayed responses, errors, and saved data.`, 'The feature handles success, delay, invalid responses, and dependency failures without misleading the user.')
  if (areas.includes('Security')) add('Security', 'Check access, session, and sensitive-data behavior', 'High', `${user} and a second user role or account are available.`, `Try “${featureName}” with changed permissions, an expired session, repeat requests, and direct navigation where relevant.`, 'Only authorized users can act, sensitive information stays protected, and unsafe repeat behavior is prevented.')
  if (areas.includes('Regression')) add('Regression', 'Protect related existing behavior', 'Medium', `${user} can access the related workflow before and after the change.`, `Run the highest-value related paths around “${featureName},” including navigation, saved state, and existing user actions.`, 'The new change does not break the adjacent experience or previously supported behavior.')

  if (/(password|login|sign in|authentication|account)/.test(normalized)) add('Security', 'Account recovery and session safety', 'High', 'Valid and invalid accounts or reset links are available.', 'Check expired or reused links, invalid credentials, lockout behavior, and session changes.', 'Sensitive details stay hidden and account recovery behavior is safe and clear.')
  if (/(payment|checkout|order|purchase|invoice|cart)/.test(normalized)) add('API / Integration', 'Transaction integrity', 'High', 'A test payment method and order data are available.', 'Check totals, tax or discounts, failed payments, duplicate submission, confirmation details, and order state.', 'Amounts and confirmation records remain accurate, including when the transaction fails or is repeated.')
  if (/(email|notification|message|alert)/.test(normalized)) add('API / Integration', 'Delivery and content', 'Medium', 'A test recipient and controllable notification condition are available.', 'Check recipients, timing, duplicate sends, content, links, and a simulated delivery failure.', 'The right person receives an accurate message once, with useful behavior when delivery fails.')
  if (/(upload|file|attachment|document|image)/.test(normalized)) add('Negative', 'File handling and recovery', 'Medium', 'Supported, unsupported, empty, and oversized files are available.', 'Try supported and invalid files, interruption, repeat upload, and retry behavior.', 'The feature gives clear progress and errors while protecting saved data from duplicates or corruption.')
  if (risk.trim()) add('Regression', `Risk focus: ${risk.trim().slice(0, 65)}`, 'High', 'The named risk can be reproduced or observed in a safe test environment.', `Target the stated risk while completing “${featureName}” under normal and failure conditions.`, 'Evidence shows whether the stated risk is reduced, accepted, or still needs a follow-up decision.')

  return cases
}

function buildClarifyingQuestions(description: string, role: string, integrations: string) {
  const featureName = description.replace(/\s+/g, ' ').trim().slice(0, 80) || 'this feature'
  return [
    `What counts as a successful outcome for “${featureName},” and how should that outcome be visible to the user?`,
    role.trim() ? `Which permissions, data states, or exceptions affect ${role.trim()}?` : 'Which user roles, permissions, or account states should behave differently?',
    integrations.trim() ? `What should happen when ${integrations.trim()} is delayed, unavailable, or returns incomplete data?` : 'Which APIs, emails, saved data, or third-party services support this flow, and how should failures appear?',
  ]
}

export function QaToolkits() {
  const [activeTool, setActiveTool] = useState<'decision' | 'cases' | 'prompts'>('decision')
  const [answers, setAnswers] = useState(() => automationQuestions.map(() => false))
  const [feature, setFeature] = useState('')
  const [userRole, setUserRole] = useState('')
  const [platform, setPlatform] = useState('')
  const [risk, setRisk] = useState('')
  const [integrations, setIntegrations] = useState('')
  const [selectedAreas, setSelectedAreas] = useState<TestArea[]>(['Functional', 'Negative', 'Accessibility'])
  const [showCases, setShowCases] = useState(false)
  const [selectedCaseIds, setSelectedCaseIds] = useState<string[]>([])
  const [copied, setCopied] = useState<string | null>(null)
  const [promptCategory, setPromptCategory] = useState('All')
  const score = answers.filter(Boolean).length
  const recommendation = useMemo(() => {
    const [, , isStable, hasReliableData] = answers
    if (!isStable || !hasReliableData) return score >= 3
      ? { title: 'Explore manually first', description: 'The workflow has value, but it needs clearer behavior or more reliable data before automation can provide trustworthy feedback.', layer: 'Exploratory testing + API or log checks', next: 'Document the workflow, clarify the unstable behavior, and create dependable data or environment setup first.' }
      : { title: 'Do not automate yet', description: 'The flow does not have enough stable, repeatable signals to justify automation work right now.', layer: 'Manual and exploratory testing', next: 'Focus on product understanding, risks, and clearer acceptance criteria before investing in automation.' }
    if (score >= 5) return { title: 'Automate now', description: 'This is a repeatable, high-value workflow with the stability and feedback timing needed for useful automation.', layer: 'API or UI smoke test', next: 'Automate the highest-risk happy path first; use API setup where possible and add UI coverage only where the user journey matters.' }
    if (score >= 3) return { title: 'Use a blended approach', description: 'Automate the stable, repeatable parts of the flow while continuing exploratory testing around change and uncertainty.', layer: 'API or component check + exploratory testing', next: 'Choose one stable core check to automate, then time-box exploration around the changing or risky edges.' }
    return { title: 'Do not automate yet', description: 'The current value or feedback signal is not strong enough to justify automation work right now.', layer: 'Manual and exploratory testing', next: 'Learn more about the user impact, change rate, and likely failure points before choosing an automation target.' }
  }, [answers, score])
  const copyPrompt = async (label: string, text: string) => {
    await navigator.clipboard?.writeText(text)
    setCopied(label)
    window.setTimeout(() => setCopied(null), 1800)
  }
  const checklist = feature.trim() || 'this feature'
  const generatedCases = useMemo(() => buildTestCases(checklist, userRole, platform, risk, integrations, selectedAreas), [checklist, userRole, platform, risk, integrations, selectedAreas])
  const clarifyingQuestions = useMemo(() => buildClarifyingQuestions(checklist, userRole, integrations), [checklist, userRole, integrations])
  const selectedCases = generatedCases.filter((testCase) => selectedCaseIds.includes(testCase.id))
  const testCaseMarkdown = `# Test case starter: ${checklist}\n\n${selectedCases.map((testCase, index) => `## ${index + 1}. ${testCase.title}\n- Description: ${testCase.description}\n- Area: ${testCase.area}\n- Priority: ${testCase.priority}\n- Preconditions: ${testCase.preconditions}\n- Steps: ${testCase.steps}\n- Expected result: ${testCase.expected}`).join('\n\n')}\n\n## Questions to clarify\n${clarifyingQuestions.map((question) => `- ${question}`).join('\n')}`
  const visiblePrompts = useMemo(() => promptCategory === 'All' ? promptLibrary : promptLibrary.filter((item) => item.category === promptCategory), [promptCategory])
  const automationSummary = `Automation decision: ${recommendation.title}. Signals: ${score}/${automationQuestions.length}. Suggested starting layer: ${recommendation.layer}. First useful step: ${recommendation.next}`
  const toggleArea = (area: TestArea) => { setSelectedAreas((current) => current.includes(area) ? current.filter((item) => item !== area) : [...current, area]); setShowCases(false) }
  const downloadTestCases = () => { const download = document.createElement('a'); download.href = URL.createObjectURL(new Blob([testCaseMarkdown], { type: 'text/markdown' })); download.download = 'its-watts-test-case-starter.md'; download.click(); URL.revokeObjectURL(download.href) }

  return <section className="toolkits section" id="tools">
    <div className="section-heading"><p className="eyebrow">QA TOOLKITS</p><h2>Useful tools.<br /><em>No fluff.</em></h2><p className="section-copy">Simple helpers for making better testing decisions before you open a ticket, write a script, or start a release.</p></div>
    <div className="tool-tabs" role="tablist" aria-label="QA tools"><button type="button" className={activeTool === 'decision' ? 'active' : ''} onClick={() => setActiveTool('decision')} role="tab" aria-selected={activeTool === 'decision'}>Automation guide</button><button type="button" className={activeTool === 'cases' ? 'active' : ''} onClick={() => setActiveTool('cases')} role="tab" aria-selected={activeTool === 'cases'}>Test case starter</button><button type="button" className={activeTool === 'prompts' ? 'active' : ''} onClick={() => setActiveTool('prompts')} role="tab" aria-selected={activeTool === 'prompts'}>QA prompts</button></div>
    <DeepDiveArticle label="QA TOOLKITS · FULL ARTICLE" title="Use tools to improve the decision—not replace it." intro="A checklist, generated test case, or AI prompt is useful when it makes the next conversation clearer and the evidence more trustworthy." sections={[["Automation guide", "Use it to decide whether a stable, repeatable workflow deserves automation. Start with user impact, change rate, test data, and feedback timing."], ["Test case starter", "Use it for a first coverage draft. Add role, platform, risk, and integrations, then compare the results with the actual requirements."], ["QA prompts", "Use prompts to expand questions and organize early thinking. Verify every assumption against the product, evidence, and team context."]]} />

    {activeTool === 'decision' && <div className="tool-panel"><div className="tool-panel-copy"><p className="eyebrow">AUTOMATION DECISION GUIDE</p><h3>Should this be automated?</h3><p>Answer six practical questions. This is a starting conversation—not a substitute for understanding the product.</p></div><div className="decision-card"><div className="decision-questions">{automationQuestions.map((question, index) => <label key={question}><input type="checkbox" checked={answers[index]} onChange={() => setAnswers((current) => current.map((answer, answerIndex) => answerIndex === index ? !answer : answer))} /><span>{question}</span><i>{answers[index] ? '✓' : '+'}</i></label>)}</div><div className="decision-result"><p className="eyebrow">RECOMMENDATION · {score}/{automationQuestions.length} SIGNALS</p><h4>{recommendation.title}</h4><p>{recommendation.description}</p><div className="decision-details"><article><span>BEST STARTING LAYER</span><b>{recommendation.layer}</b></article><article><span>FIRST USEFUL STEP</span><b>{recommendation.next}</b></article></div><button type="button" onClick={() => copyPrompt('automation-summary', automationSummary)}>{copied === 'automation-summary' ? 'Copied ✓' : 'Copy decision summary ↗'}</button></div><p className="automation-caution">Automation supports quality; it does not replace exploratory testing, accessibility checks, or product understanding.</p></div></div>}

    {activeTool === 'cases' && <div className="tool-panel"><div className="tool-panel-copy"><p className="eyebrow">TEST CASE STARTER</p><h3>Turn a feature into test cases.</h3><p>Add only the context you know. The starter creates a practical first draft—not a substitute for product knowledge.</p></div><div className="case-card"><label htmlFor="feature">FEATURE OR USER STORY</label><textarea id="feature" value={feature} onChange={(event) => { setFeature(event.target.value); setShowCases(false) }} placeholder="Example: A customer can reset their password using an email link." /><div className="case-form-grid"><label>USER ROLE<input value={userRole} onChange={(event) => { setUserRole(event.target.value); setShowCases(false) }} placeholder="Example: account owner" /></label><label>PLATFORM<input value={platform} onChange={(event) => { setPlatform(event.target.value); setShowCases(false) }} placeholder="Example: mobile web" /></label><label>KNOWN RISK<input value={risk} onChange={(event) => { setRisk(event.target.value); setShowCases(false) }} placeholder="Example: duplicate order" /></label><label>INTEGRATION<input value={integrations} onChange={(event) => { setIntegrations(event.target.value); setShowCases(false) }} placeholder="Example: payment API" /></label></div><fieldset className="test-area-picker"><legend>INCLUDE TEST AREAS</legend><div>{testAreas.map((area) => <label key={area}><input type="checkbox" checked={selectedAreas.includes(area)} onChange={() => toggleArea(area)} /><span>{area}</span></label>)}</div></fieldset><button type="button" className="button primary" onClick={() => { setSelectedCaseIds(generatedCases.map((testCase) => testCase.id)); setShowCases(true) }}>Generate test cases <span aria-hidden="true">↗</span></button>{showCases && <div className="case-results"><div className="case-result-heading"><p className="eyebrow">TEST CASE STARTER FOR {checklist.toUpperCase()}</p><p>{selectedCases.length} of {generatedCases.length} selected</p></div><div className="generated-cases">{generatedCases.map((testCase) => <article key={testCase.id}><label><input type="checkbox" checked={selectedCaseIds.includes(testCase.id)} onChange={() => setSelectedCaseIds((current) => current.includes(testCase.id) ? current.filter((id) => id !== testCase.id) : [...current, testCase.id])} /><span>Select test case</span></label><p className="eyebrow">{testCase.area} · {testCase.priority} PRIORITY</p><h4>{testCase.title}</h4><p className="generated-case-description">{testCase.description}</p><dl><div><dt>Preconditions</dt><dd>{testCase.preconditions}</dd></div><div><dt>Steps</dt><dd>{testCase.steps}</dd></div><div><dt>Expected result</dt><dd>{testCase.expected}</dd></div></dl></article>)}</div><section className="clarifying-questions"><p className="eyebrow">QUESTIONS TO CLARIFY</p><ul>{clarifyingQuestions.map((question) => <li key={question}>{question}</li>)}</ul></section><div className="case-actions"><button type="button" disabled={!selectedCases.length} onClick={() => copyPrompt('test-case-summary', testCaseMarkdown)}>{copied === 'test-case-summary' ? 'Copied ✓' : 'Copy selected ↗'}</button><button type="button" disabled={!selectedCases.length} onClick={downloadTestCases}>Download Markdown ↧</button></div></div>}</div></div>}

    {activeTool === 'prompts' && <div className="tool-panel"><div className="tool-panel-copy"><p className="eyebrow">QA PROMPT LIBRARY</p><h3>Prompts that improve the work.</h3><p>Choose a real QA moment, add your product context, and review the output before you rely on it.</p></div><div><div className="prompt-filters" role="group" aria-label="Filter QA prompts by category">{promptCategories.map((category) => <button type="button" key={category} className={promptCategory === category ? 'active' : ''} onClick={() => setPromptCategory(category)} aria-pressed={promptCategory === category}>{category}</button>)}</div><p className="prompt-swipe-hint">Swipe or scroll to explore {visiblePrompts.length} prompts <span aria-hidden="true">→</span></p><div className="prompt-list-tool">{visiblePrompts.map((item) => <article key={item.label}><p>{item.category} · {item.label}</p><div className="prompt-meta"><span><b>When to use</b>{item.when}</span><span><b>Paste in</b>{item.paste}</span><span><b>You’ll get</b>{item.outcome}</span></div><pre>{item.prompt}</pre><button type="button" onClick={() => copyPrompt(item.label, item.prompt)}>{copied === item.label ? 'Copied ✓' : 'Copy prompt ↗'}</button></article>)}</div></div></div>}
  </section>
}
